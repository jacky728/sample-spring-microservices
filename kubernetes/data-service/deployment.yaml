apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: data-service
  name: data-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: data-service
  template:
    metadata:
      labels:
        app: data-service
    spec:
      containers:
      - image: data-service:1.0-SNAPSHOT
        name: data-service
        resources:
          limits:
            cpu: 200m
            memory: 800Mi
          requests:
            cpu: 50m
            memory: 500Mi
        ports:
          - containerPort: 8081
        livenessProbe:
          initialDelaySeconds: 90
          httpGet:
            path: /actuator/health/liveness
            port: 8081
        readinessProbe:
          initialDelaySeconds: 90
          httpGet:
            path: /actuator/health/readiness
            port: 8081
        lifecycle:  # Enable graceful shutdown
          preStop:
            exec:
              command: ["sh", "-c", "sleep 10"]
        volumeMounts:
          - name: config-volume
            mountPath: /workspace/config

      volumes:
        - name: config-volume
          configMap:
            name: app-graceful-shutdown